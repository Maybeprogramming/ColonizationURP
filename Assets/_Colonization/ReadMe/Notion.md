---
	# Версия 1 от 28 апреля 2026, 22:02

	Ревью (Денис Калужин)

	+1. private static int ID = 0; - не по нотации. И статика тут излишне.
	/ПС 1. Статику убрал, сделал поле приватным _id;
	+2. Resource 
		+2.1 TryClaim - сам ресурс ничего не заявляет, имя не отражает суть. 
		+2.2 IsClaimed - это вообще не ответственность ресурса. 
			Он не знает, что его собрать или занять могут. 
			Весь менеджмент на уровне базы происходит. 
			Менеджментом занимается база, а точнее какая-то общая БД, которая получает сигналы от сканеров 
			(напрямую или посредством базы) и хранит 2 списка: найденные и занятые ресурсы. 
			Когда базе нужно отправить бота, она сначала запрашивает у БД свободный ресурс и 
			если получает его, то отдает в работу боту, а БД перекладывает этот ресурс в список занятых. 
			Это станет важно, когда баз будет несколько и их сканеры будут пересекаться.
		+2.3 Ресурс не должен знать про пул. Это не его ответственность. И циклические зависимости - это зло. 
			Зависимости строим строго иерархически. Сущности верхнего уровня могут знать про сущности низших уровней. 
			Нижние про верхних знать не могут, это резко сужает их область применения. 
			В данном случае вы не можете даже для теста вытащить на сцену ресурс не имея спавнера, все  в ошибки упадет. Не надо так.
			/ПС 2.3 теперь ресурс не знает про пул
		+2.4 PutToPool - ресурс не знает ни про какой пул. давайте имена из предметной области и согласно информации доступной самому объекту. 
		/ПС 2.4 возврат в пул после срабатывая события ResourseAdded у базы на которое подписывается спавнер и сам возвращает ресурс в пул.
	+3. ResourceCounter - вспоминаем самую первую задачу на счетчик и учимся правильно выстраивать архитектуру и 
		разделяем скрипты логики и визуала. Тут несколько статей на тему https://t.me/KaDR_gamedev/169
		/ПС 3. Отделил логику от визуала
	+4. ResourceScanner
		+4.1 не надо постоянно пересоздавать WaitForSeconds в цикле, если время не меняется. Создайте его один раз перед циклом.
		/ПС 4.1 исправил
		+4.2 Resource resource = collider.GetComponent<Resource>(); if (resource != null - это TryGetComponent
		/ПС 4.2 исправил
		+4.3 Сканер не должен хранить найденные ресурсы, это не его ответственность. Он нашел и вернул. На этом все.
		+4.4 resource.IsClaimed == false && _resources.Contains(resource) == false) - аналогично. Фильтрацией занимается уже кто-то выше. 
		+4.5 TryGetResource - метод БД, а не сканера. См. п. 2.2.
	+5. Vector3.Distance (он же magnitude) - Это очень затратная операция из-за вычисления корня в конце. 
		У процессора с этим туго. Везде где возможно лучше вместо дистанции использовать sqrMagnitude, 
		это даже в документации написано (просто сравниваешь не с самой дистанцией, а с ее квадратом) https://t.me/KaDR_gamedev/83
		/ПС 5. использовал Vector3.SqrMagnitude
	+6. protected virtual void Update() - перегружать методы юинити - плоха практика. 
		Если сильно надо, то лучше создать свой метод, который будет вызваться в апдейте и перегружать его.
		/ПС 6. protected virtual void UpdateState() добавил свой виртуальный метод, у метода юнити убрал перегрузку.
	+7. Для читаемости состояния обычно именуют с суффиксом State.
		/ПС 7. добавил суффикс

---